// ============================================
// Enums
// ============================================

enum FileProvider {
  UPLOADTHING
}

enum Role {
  USER
  ADMIN
  SUPER_ADMIN
}

enum TaskStatus {
  TODO
  IN_PROGRESS
  DONE
}

enum NotificationType {
  TASK_CREATED
  TASK_UPDATED
  TASK_ASSIGNED
}
